import React from 'react';
import { BrowserRouter as Router } from 'react-router-dom';

const Routes = () => {
  return;
  <Router>
    <switch>
      <Route></Route>
    </switch>
  </Router>;
};

export default Routes;

// tag Router gunanya untuk membungkus semua route
// di import dari react router dom, untuk mengatur perpindahan
